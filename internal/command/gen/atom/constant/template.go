package constant

const (
	ATOM_PREFIX = "atom"
)

var (
	SERVICE_TEMPLATE    = ATOM_PREFIX + "/service"
	MODEL_TEMPLATE      = ATOM_PREFIX + "/model"
	REPOSITORY_TEMPLATE = ATOM_PREFIX + "/repository"
)
